<!-- $(function(){
		var doScroll = true;
		var browserRE = /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i; 
		if(browserRE.test(navigator.userAgent)){
				doScroll = false;
		};

		
		$(".text-center .btn").click(function(ev){
				if(doScroll == true){
						ev.preventDefault();
						var id = $(this).attr("href");
						$.scrollTo(id, id == "#signUp" ? 650 : 950);
				}
		});

		-->